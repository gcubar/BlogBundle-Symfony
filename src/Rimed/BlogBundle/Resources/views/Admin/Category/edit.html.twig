{% extends "RimedBlogBundle::layout.html.twig" %}

{% block title %}Editando la categoría - {{ category.name }}{% endblock %}

{% block content %}
<form action="{{ path('blog_admin_category_edit', { 'id': category.id }) }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit" value="Guardar cambios" />
</form>
<a href="{{ path('blog_admin_category_index') }}">Volver al listado</a>
{% endblock %}
